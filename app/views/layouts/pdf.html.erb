<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    /* Estilos para o layout do PDF */
    html, body {
      height: 100%;
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
    }

    .page {
      position: relative;
      min-height: 100%; /* Garante que a página ocupe pelo menos a altura total */
      padding-bottom: 0px; /* Espaço para o rodapé */
    }

    .header {
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.logo {
  max-height: 100px; /* Ajuste conforme necessário */
  margin-right: 20px; /* Espaçamento entre a logo e a informação da escala */
}

.escala-info {
  display: flex;
  flex-direction: column;
}

.escala-info p {
  margin: 0; /* Remover margem padrão dos parágrafos */
}

    .content {
      margin-top: 40px; /* Espaço suficiente para o cabeçalho */
      padding: 10px;
      /* Ajuste se necessário para o layout */
    }

    .footer {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      height: 30px;
      background-color: #f2f2f2;
      border-top: 1px solid #ccc;
      text-align: center;
      line-height: 30px;
      font-size: 12px;
      z-index: 1000; /* Garante que o rodapé fique acima do conteúdo */
    }

    @page {
      margin: 10px; /* Remove margens padrão da página */
    }
  </style>
</head>
<body>
  
    <div class="header">
      <%= image_tag image_url('CronoMEDLOGO.png'), alt: 'Logo do MedicoManage' %>
      
    </div>
    <div class="page">
    
    <div class="content">
      <%= yield %> <!-- Aqui será inserido o conteúdo da view específica -->
    </div>

    <div class="footer">
     
    </div>
  </div>
</body>
</html>
