<div id="<%= dom_id jornada %>" class="card mb-3 border border-secondary mx-auto" style="width: 80%;">
  <div class="card-body p-3">
    <p class="card-text">
      <strong>Data:</strong>
      <%= jornada.data.strftime("%d/%m/%Y") %>
    </p>
    <p class="card-text">
      <strong>Turno:</strong>
      <%= jornada.turno %>
    </p>
    <p class="card-text">
      <strong>Médico:</strong>
      <%= User.find(jornada.user_id).first_name %>
    </p>
    <p class="card-text">
      <strong>Escala:</strong>
      <%= Escala.find(jornada.escala_id).setor %> - <%= Escala.find(jornada.escala_id).hospital.nome_hospital %>
    </p>
  </div>
</div>
